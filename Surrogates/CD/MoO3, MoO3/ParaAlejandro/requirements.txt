# Dependencias del proyecto (red neuronal CD + paquete GTMM)
# Instalar con:  pip install -r requirements.txt
#
# El proyecto usa Keras 3 (el que trae TensorFlow 2.16+). No hace falta
# tf-keras ni TF_USE_LEGACY_KERAS: el codigo entrena, guarda y carga con Keras 3.

numpy>=1.24
scipy>=1.10
pandas>=2.0
matplotlib>=3.7
tensorflow>=2.16
