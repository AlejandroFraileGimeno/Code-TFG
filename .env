# Variables que VS Code carga al ejecutar/depurar (python.envFile)
# Silencian los logs ruidosos de TensorFlow. El proyecto usa Keras 3
# (el modelo se entrena y se guarda con Keras 3), asi que NO se usa
# TF_USE_LEGACY_KERAS.
TF_CPP_MIN_LOG_LEVEL=2
TF_ENABLE_ONEDNN_OPTS=0
